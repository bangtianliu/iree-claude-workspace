---
description: Kill stale VSCode remote server and clean sockets
allowed-tools: Bash(pkill:*), Bash(rm:*), Bash(./scripts/nuke-vscode.sh:*)
---

Run the nuke script:
```bash
./scripts/nuke-vscode.sh
```

Then tell user to reconnect from VSCode.
